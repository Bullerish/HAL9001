# .NET build output
bin/
obj/
publish-linux/
publish/

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo

# Local environment / secrets (the Anthropic API key will live here in a later step)
.env
appsettings.*.local.json
